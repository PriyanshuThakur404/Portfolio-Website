I updated the site markup and styles to use a profile image at `assets/profile.jpg`.

To display your attached photo on the site:
1. Save your photo as `assets/profile.jpg` (recommended 200×200 or square). 
2. Open `index.html` — the hero now uses `assets/profile.jpg` and the photo will appear automatically.

If you'd like, reply here and I will add the attached image file into `assets/profile.jpg` for you.